# Этап A: исправления после ревью, 26 сентября 2026

Ветка: `codex/repository-structure`. Все 15 замечаний приняты; ниже — изменения,
проверки и границы результата. Удаление переходных путей и публикация не выполнялись.

## Что исправлено

| № | Результат | Проверка |
| --- | --- | --- |
| 1 | Компактный Creative Mode снова без автоматического приложения. Полные приложения выбраны в шаблонах через `{{appendix: ...}}`. Убраны лишние пустые строки Prompt Engineer и висячие ссылки на references в текстах для вставки. | `scripts/test-chat-prompts.py`: шесть golden-хэшей тел из `e2c7d4f` и точный список допустимых замен; без нормализации пробелов. |
| 2 | NOTICE хранит канонические пути плагина; для standalone сборщик убирает префикс `skills/<name>/`. Пути scripts и assets корректны в обоих пакетах. | В распакованных ZIP проверяются локальные Markdown-ссылки и пути к ресурсам в LICENSE, NOTICE, SKILL. |
| 3 | SKILL ведёт на `scripts/README.md` для Chromium. В README явно разделены Python 3.10+, базовый runtime и отдельная установка Playwright/Chromium. | Проверка ресурсов распакованного пакета; INSTALL отсутствует в ZIP. |
| 4 | Возвращены полезные советы из старых INSTALL: активация, неполная папка, имя skill, seed/температура/независимость прогонов, ограничения модели, валидатор CRAFT+ и JSON-режим. | Troubleshooting в обеих версиях общей инструкции; ссылки на диагностику установленного skill. |
| 5 | В корневых README есть карта путей, включая шаблон и INSTALL. Все legacy README помечают `.skill` как замороженные сборки. | Три архива побайтно совпадают с `4128fe6`; семь переходных ссылок исправны; SHA-256 резервных файлов совпадают с manifest. |
| 6 | EN/RU имеют одинаковые разделы, таблицы, список чат-файлов, troubleshooting и источники. Аудиторский раздел заменён пользовательскими пояснениями. Дубль RU-ссылки из EN README удалён. | Bash-блоки идентичны и исполняются в обоих языковых документах. |
| 7 | Исходное предложение явно помечено как заменённое; итоговый отчёт объясняет отказ от `shared` и брендовых деревьев. | Исторические факты и исходный текст сохранены, ссылки ведут на принятое решение. |
| 8 | Существующая цепочка через общую папку принимается, если разрешается в канонический источник. | Bash/Zsh, абсолютные и относительные цепочки; без `readlink -f`. |
| 9 | У исправных legacy-fixtures теперь существуют реальные цели; битые случаи отдельные. Проверяются сами опубликованные команды. | `test-install-links.sh`, `test-install-docs.py`: обе оболочки, оба документа, три блока, шесть состояний установки, повторный запуск. Переменные, cwd и опции внешней оболочки сохраняются. |
| 10 | Добавлен регрессионный тест устаревших входов/результатов генерации. | `test-build-readonly.py`: template, reference, version, LICENSE, listing-extra, interface, INSTALL и оба JSON; оба сборщика отказывают, Git status и снимок файлов не меняются. Успешная сборка проверяется без legacy-каталогов. |
| 11 | gen_checklist проверяет checklist и оба JSON-двойника; оба сборщика вызывают его `--check`. | Повреждение каждого JSON обнаруживается; три результата помечены `linguist-generated`. |
| 12 | Проверки skill запускаются из распакованного каталога; submission-тесты — из распакованного plugin. | Проверяются все шесть ZIP, SHA256SUMS, submission fixtures и тесты skill в обоих видах пакета. |
| 13 | `PYTHONDONTWRITEBYTECODE=1`; сравнение Git status включает игнорируемые файлы, исключая только dist. | Итоговый `check.sh` не меняет исходники и не создаёт неожиданных файлов. |
| 14 | Каталоги версий имеют 0755; каждый запуск обновляет оба архива, даже с `--kind skill` или `plugin`. | В тесте старые ZIP предварительно испорчены; обе команды восстанавливают актуальную пару. Хэши совпадают при другом порядке файлов, TZ, mtime, umask и правах. |
| 15 | Синхронизатор манифестов явно использует UTF-8 и сообщает все устаревшие файлы. | Тест одновременно портит три адаптера и требует все три пути в диагностике без записи. |

## Уточнения к ревью

- Русские подписи внутри английского полного Creative Mode и висячие ссылки
  существовали уже в `e2c7d4f`; это не новый дефект объединённого генератора.
  Тем не менее их исправление необходимо для текста, вставляемого в чат. Шесть
  точечных замен ссылок и новая команда пересборки отражены в CHANGELOG и golden-fixture.
  Остальные байты тел промптов, включая пустые строки, восстановлены.
- Статистические числа и устаревшие инструкции пересборки `.skill` из старого
  troubleshooting не переносились как новые гарантии. Сохранены диагностические
  действия и ссылки на канонические ограничения методики.
- `v1.4.1-1-g4128fe6` — проверенное обозначение Git-ревизии прежних сборок,
  а не утверждение, что все три skill имели одинаковую продуктовую версию.
  Старые архивы не содержат единого поля версии; точная идентификация — commit
  и контрольные суммы, а не приписанная им текущая версия плагина.
- Для истории выбран разрешённый в замечании вариант: явная пометка superseded
  и ссылка на итог, без переписывания первоначальных выводов задним числом.

## Проверки и следующий шаг

Локальная среда: macOS, Python 3.14.5. Команда:

```bash
PYTHONPATH=/private/tmp/pepperskills-ci-deps bash scripts/check.sh
```

Каталог в PYTHONPATH содержит зависимости из `scripts/requirements-build.txt`;
в CI они устанавливаются на этапе подготовки. Офлайн-набор блокирует и записывает
сетевые попытки, в том числе в дочерних Python-процессах. Команды менеджеров,
живые сайты, браузерная приёмка и загрузка ZIP клиентом не выдаются за офлайн-тесты.

Итоговый локальный прогон: **PASS**. Дополнительно проверены исходные legacy-байты,
резервные SHA-256 и семь переходных симлинков. Локальный прогон не доказывает
совместимость Linux/Python 3.10/3.12: это проверяют три job в GitHub workflow,
а четвёртый сравнивает хэши между средами.

Push и GitHub CI в этой доработке ещё не выполнялись. По разделу задания
«Готовность (после согласования с владельцем)» следующий шаг — согласовать push
ветки, получить успешные четыре job и добавить ссылку на run. Клиентская приёмка,
выпуск и удаление старых путей остаются отдельными действиями.
